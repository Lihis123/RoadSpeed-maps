{
  "name": "roadspeed-build-speeddb",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Builds the offline speed limit database from an OpenStreetMap extract.",
  "scripts": {
    "test": "node --test *.test.mjs"
  },
  "dependencies": {
    "better-sqlite3": "^12.11.1",
    "proj4": "^2.20.9",
    "shapefile": "^0.6.6"
  }
}
